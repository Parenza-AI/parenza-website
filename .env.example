# Public, build-time env vars (inlined into the static output — no secrets here).
# Base URL of the Ship API the early-access form posts to (POST /v1/waitlist).
# Empty disables submission and the form falls back to the mailto path.
PUBLIC_PARENZA_API_URL=https://app.shipplatform.ai
